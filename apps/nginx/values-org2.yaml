namespace: default
replicaCount: 1
appLabel: nginx-custom-org2

image:
  repository: my-nginx-custom-org2
  tag: "1.0"

container:
  name: nginx-custom
  port: 80

service:
  type: ClusterIP
  port: 80
  targetPort: 80

ingress:
  enabled: true
  className: nginx
  host: test2.local
  path: /
